-- Roblox Coin Farm Script (Executor Version)
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui") -- Используем CoreGui, чтобы GUI не пропадал после смерти

local player = Players.LocalPlayer
local coinName = "Coin_Server"
local speed = 1.5
local farmEnabled = false
local currentTarget = nil

-- Создание GUI (Защита от удаления при смерти)
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "CoinAutoFarm_By_AI"
-- Пытаемся засунуть в CoreGui, если экзекутор позволяет, иначе в PlayerGui
local success, err = pcall(function()
    screenGui.Parent = CoreGui
end)
if not success then
    screenGui.Parent = player:WaitForChild("PlayerGui")
end

-- Главное окно
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 140, 0, 60)
mainFrame.Position = UDim2.new(0.5, -70, 0.4, -30)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true -- Включаем стандартный драг для экзекуторов
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 10)
corner.Parent = mainFrame

local stroke = Instance.new("UIStroke")
stroke.Color = Color3.fromRGB(60, 60, 60)
stroke.Thickness = 2
stroke.Parent = mainFrame

-- Кнопка Тогл
local toggleBtn = Instance.new("TextButton")
toggleBtn.Size = UDim2.new(0.85, 0, 0.7, 0)
toggleBtn.Position = UDim2.new(0.075, 0, 0.15, 0)
toggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
toggleBtn.Text = "COIN: OFF"
toggleBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.TextSize = 14
toggleBtn.AutoButtonColor = true
toggleBtn.Parent = mainFrame

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 8)
btnCorner.Parent = toggleBtn

-- Функция поиска ближайшей монеты
local function getClosestCoin()
    local char = player.Character
    if not char then return nil end
    local root = char:FindFirstChild("HumanoidRootPart")
    if not root then return nil end

    local closest = nil
    local dist = math.huge

    for _, obj in pairs(workspace:GetDescendants()) do
        if obj.Name == coinName and obj:IsA("BasePart") then
            local d = (root.Position - obj.Position).Magnitude
            if d < dist then
                dist = d
                closest = obj
            end
        end
    end
    return closest
end

-- Логика кнопки
toggleBtn.MouseButton1Click:Connect(function()
    farmEnabled = not farmEnabled
    if farmEnabled then
        toggleBtn.Text = "COIN: ON"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(50, 180, 50)
    else
        toggleBtn.Text = "COIN: OFF"
        toggleBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
        currentTarget = nil
    end
end)

-- Основной цикл (Heartbeat работает лучше всего для изменения CFrame)
RunService.Heartbeat:Connect(function()
    if not farmEnabled then return end
    
    local char = player.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    
    if root then
        -- Если цели нет или она удалена из игры, ищем новую
        if not currentTarget or not currentTarget.Parent or not currentTarget:IsDescendantOf(workspace) then
            currentTarget = getClosestCoin()
        end

        if currentTarget then
            -- Направление полета
            local dir = (currentTarget.Position - root.Position).Unit
            
            -- Плавное перемещение CFrame (игнорирует физику, летит сквозь стены)
            root.CFrame = root.CFrame + (dir * speed)
            
            -- Поворот персонажа в сторону монеты
            root.CFrame = CFrame.lookAt(root.Position, currentTarget.Position)
        end
    end
end)
